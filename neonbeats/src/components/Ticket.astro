
<section class="seccion-ticket" aria-label="Zona de entradas">
    <div class="wrapper">
        <img
            class="imagen-ticket"
            src="/images/imagenTicket.png"
            alt="Ticket neon del festival"
            loading="lazy"
        />
        <div id="degradadoAbajo"></div>
        <a href=""><img id="ticketPrueba" src="/images/ot.png" alt=""></a>
    </div>
    
    <div id="degradado"></div>
    <h1 id="compra">Compra tu ticket aquí</h1>
    
</section>

<style>
   .seccion-ticket {
    position: relative;
	width: 100%;
	margin: 0;
    min-height: 100vh;
    height: 100vh;
    display: flex;
	position: relative;   
	overflow: hidden;
    justify-content: center;
    align-items: center;
}

.imagen-ticket {
	width: 100%;
    height: 100%;
    display: block;
    object-fit: cover;
}

#ticketPrueba{
    position: absolute;
    top: 47%;        
    left: 50%;       
    width: 32%;     
    transform: translate(-50%, -50%);
    z-index: 2;
    transition: transform 0.3s ease;
    border-radius: 20px;
}

#ticketPrueba:hover{
    transform: translate(-50%,-50%)scale(1.1);
}

.wrapper{
    position: relative;
    width: 70%;
    aspect-ratio: 16/9;
    margin: 0 auto;
}
#degradadoAbajo {
    position: absolute;
    bottom: 0;          
    left: 0;
    width: 100%;
    height: 20vh;       
    
    background: linear-gradient(
        to top,          
        rgba(0, 0, 0, 1) 0%,    
        rgba(0, 0, 0, 0) 80%   
    );
}

.seccion-ticket::after {
	content: "";
	position: absolute;
	inset: 0;
	pointer-events: none;

background: radial-gradient(
    circle at center,
    rgba(0,0,0,0) 10%,     
    rgba(0,0,0,0.8) 40%,   
    rgba(0,0,0,1) 80%      
  );
}

#degradado{
    width: 100%;
    height: 8em;
    position: absolute;
    top: 0;
    background: linear-gradient(
    to bottom,
    rgba(0,0,0,0.85) 0%,
    rgba(0,0,0,0.45) 35%,
    rgba(0,0,0,0.15) 65%,
    rgba(0,0,0,0.0) 100%
  );
}

#compra{
    position: absolute;
    top: 25%;
    left: 50%;
    font-size: 40px;
    transform: translate(-50%, -50%);
    z-index: 2;
}


</style>